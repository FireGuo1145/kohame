import { useEffect, useState } from "react"
import { useLocation, useNavigate, useParams } from "react-router-dom"
import { Button } from "@/components/ui/button"
import { Avatar, Loading, PageMessage, RepoCards } from "@/components/forge-ui"
import { api, when } from "@/lib/forge-api"
import type { Organization, Profile, Repository, User } from "@/lib/forge-types"

function ProfilePage({ username, currentUser, onOpen }: { username: string; currentUser: User | null; onOpen: (name: string) => void }) {
  const navigate = useNavigate()
  const location = useLocation()
  const [profile, setProfile] = useState<Profile | null>(null)
  const [repos, setRepos] = useState<Repository[]>([])
  const [message, setMessage] = useState("")
  const [sending, setSending] = useState(false)
  const [organizations, setOrganizations] = useState<Organization[]>([])
  useEffect(() => { void Promise.all([api<Profile>(`/api/users/${username}`), api<Repository[]>(`/api/users/${username}/repos`)]).then(([p, r]) => { setProfile(p); setRepos(r) }).catch((cause: unknown) => setMessage(cause instanceof Error ? cause.message : "无法加载个人主页。")) }, [username])
  useEffect(() => { if (currentUser?.username === username) void api<Organization[]>("/api/scopes").then((items) => setOrganizations(items.filter((item) => item.name !== username))).catch(() => setOrganizations([])) }, [currentUser, username])
  if (message) return <PageMessage title="个人主页" message={message} />
  if (!profile) return <Loading />
  const own = currentUser?.username === username
  const tab = new URLSearchParams(location.search).get("tab") || "overview"
  return <div className="mx-auto max-w-7xl px-5 py-9"><section className="rounded-2xl border border-zinc-200 bg-white p-6 dark:border-zinc-800 dark:bg-zinc-900"><div className="flex items-center gap-4"><Avatar name={username} src={profile.avatarUrl} size="lg" /><div className="min-w-0 flex-1"><h1 className="text-2xl font-semibold">{profile.displayName || username}</h1><p className="mt-1 text-sm text-zinc-500">@{username} · 加入于 {when(profile.createdAt)}</p></div>{currentUser && !own && <Button variant={profile.followed ? "secondary" : "outline"} onPress={async () => { try { const result = await api<{ followed: boolean; followers: number }>(`/api/users/${username}/follow`, { method: "POST" }); setProfile({ ...profile, ...result }) } catch (cause) { setMessage(cause instanceof Error ? cause.message : "操作失败。") } }}>{profile.followed ? "已关注" : "关注"}</Button>}</div>{profile.bio && <p className="mt-5 max-w-2xl text-sm leading-6 text-zinc-700 dark:text-zinc-300">{profile.bio}</p>}<div className="mt-5 flex flex-wrap gap-6 text-sm"><span><strong>{profile.repositories}</strong> 个仓库</span><span><strong>{profile.stars}</strong> 个 Star</span><button onClick={() => navigate(`/${username}/followers`)} className="hover:text-sky-700 hover:underline"><strong>{profile.followers}</strong> 位关注者</button><button onClick={() => navigate(`/${username}/following`)} className="hover:text-sky-700 hover:underline"><strong>{profile.following}</strong> 正在关注</button>{profile.location && <span>📍 {profile.location}</span>}{profile.website && <a className="text-sky-700 hover:underline dark:text-sky-300" href={profile.website} target="_blank" rel="noreferrer">个人网站</a>}</div>{own && !currentUser.emailVerified && <div className="mt-5 flex flex-wrap items-center justify-between gap-3 rounded-xl border border-amber-200 bg-amber-50 p-3 text-sm text-amber-800"><span>邮箱尚未验证，部分协作通知可能无法送达。</span><Button size="sm" variant="outline" isDisabled={sending} onPress={async () => { setSending(true); try { await api("/api/auth/verification", { method: "POST" }) } catch (cause) { setMessage(cause instanceof Error ? cause.message : "发送失败") } finally { setSending(false) } }}>重新发送验证邮件</Button></div>}</section><nav className="mt-6 flex gap-1 border-b border-zinc-200 dark:border-zinc-800"><button onClick={() => navigate(`/${username}`)} className={`border-b-2 px-3 py-2 text-sm ${tab === "overview" ? "border-emerald-600 font-medium" : "border-transparent text-zinc-500"}`}>概览</button><button onClick={() => navigate(`/${username}?tab=repositories`)} className={`border-b-2 px-3 py-2 text-sm ${tab === "repositories" ? "border-emerald-600 font-medium" : "border-transparent text-zinc-500"}`}>仓库 {repos.length}</button>{own && <button onClick={() => navigate(`/${username}?tab=organizations`)} className={`border-b-2 px-3 py-2 text-sm ${tab === "organizations" ? "border-emerald-600 font-medium" : "border-transparent text-zinc-500"}`}>组织 {organizations.length}</button>}</nav>{tab === "organizations" && own ? <section className="mt-8"><h2 className="mb-3 font-semibold">组织</h2><div className="grid gap-3 sm:grid-cols-2">{organizations.map((organization) => <button key={organization.name} onClick={() => navigate(`/${organization.name}`)} className="rounded-lg border border-zinc-200 bg-white p-4 text-left hover:border-violet-400 dark:border-zinc-800 dark:bg-zinc-900"><strong>{organization.name}</strong><p className="mt-1 text-sm text-zinc-500">{organization.followers} 位关注者</p></button>)}{!organizations.length && <p className="text-sm text-zinc-500">你还没有加入组织。</p>}</div></section> : <section className="mt-8"><h2 className="mb-3 font-semibold">{tab === "repositories" ? "所有仓库" : "仓库"}</h2><RepoCards repos={repos} onOpen={onOpen} /></section>}</div>
}

export default function ProfileRoutePage({ user, onOpen }: { user: User | null; onOpen: (name: string) => void }) {
  const { username } = useParams()
  return username ? <ProfilePage username={username} currentUser={user} onOpen={onOpen} /> : null
}
