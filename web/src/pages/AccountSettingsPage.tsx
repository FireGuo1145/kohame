import { useEffect, useState, type FormEvent } from "react"
import { Bell, CircleUserRound, MailCheck, Shield } from "lucide-react"

import { Button } from "@/components/ui/button"

type User = { username: string }
type PersonalSettings = { username: string; email: string; verified: boolean; displayName: string; bio: string; location: string; website: string }
type Section = "profile" | "security" | "notifications"

async function api<T>(url: string, init?: RequestInit): Promise<T> { const response = await fetch(url, { ...init, headers: { "Content-Type": "application/json", ...init?.headers } }); if (!response.ok) { const body = await response.json().catch(() => ({})); throw new Error(body.error || "请求失败。") }; return response.status === 204 ? undefined as T : response.json() }
function Field({ label, value, onChange, placeholder }: { label: string; value: string; onChange: (value: string) => void; placeholder: string }) { return <label className="block text-sm font-medium">{label}<input value={value} onChange={(event) => onChange(event.target.value)} placeholder={placeholder} className="mt-1.5 h-10 w-full rounded-lg border border-zinc-200 bg-transparent px-3 text-sm outline-none focus:border-zinc-500 dark:border-zinc-700" /></label> }

export function AccountSettingsPage({ user }: { user: User | null }) {
  const [value, setValue] = useState<PersonalSettings | null>(null); const [message, setMessage] = useState(""); const [saving, setSaving] = useState(false); const [active, setActive] = useState<Section>("profile")
  useEffect(() => { if (!user) return; void api<PersonalSettings>("/api/user/settings").then(setValue).catch((cause: unknown) => setMessage(cause instanceof Error ? cause.message : "无法加载个人设置。")) }, [user])
  if (!user) return <div className="mx-auto max-w-3xl px-5 py-9"><h1 className="text-2xl font-semibold">个人设置</h1><p className="mt-5 rounded-lg bg-red-50 p-4 text-sm text-red-700">请先登录后再管理个人资料。</p></div>
  if (!value) return <main className="grid min-h-[50svh] place-items-center text-sm text-zinc-500">加载中…</main>
  const save = async (event: FormEvent) => { event.preventDefault(); setSaving(true); try { setValue(await api<PersonalSettings>("/api/user/settings", { method: "PATCH", body: JSON.stringify(value) })); setMessage("个人资料已保存。") } catch (cause) { setMessage(cause instanceof Error ? cause.message : "保存失败。") } finally { setSaving(false) } }
  const nav: { id: Section; label: string; icon: typeof CircleUserRound }[] = [{ id: "profile", label: "公开资料", icon: CircleUserRound }, { id: "security", label: "账户与安全", icon: Shield }, { id: "notifications", label: "通知", icon: Bell }]
  const title = nav.find((item) => item.id === active)?.label || "个人设置"
  return <div className="mx-auto grid max-w-6xl gap-8 px-5 py-9 md:grid-cols-[200px_1fr]"><aside className="h-fit rounded-lg border border-zinc-200 bg-white p-2 text-sm dark:border-zinc-800 dark:bg-zinc-900"><p className="px-3 py-2 text-xs font-semibold uppercase tracking-[0.12em] text-zinc-400">个人设置</p>{nav.map(({ id, label, icon: Icon }) => <button key={id} onClick={() => { setActive(id); setMessage("") }} className={`flex w-full items-center gap-2 rounded-md px-3 py-2 text-left ${active === id ? "bg-zinc-900 font-medium text-white dark:bg-zinc-100 dark:text-zinc-950" : "text-zinc-600 hover:bg-zinc-100 dark:text-zinc-400 dark:hover:bg-zinc-800"}`}><Icon className="size-3.5" />{label}</button>)}</aside><form onSubmit={save} className="max-w-2xl space-y-7"><div><h1 className="text-2xl font-semibold">{title}</h1><p className="mt-1 text-sm text-zinc-500">管理你在 Kohame 上显示的身份与账户信息。</p></div>{active === "profile" && <section className="space-y-4 rounded-lg border border-zinc-200 bg-white p-6 dark:border-zinc-800 dark:bg-zinc-900"><Field label="显示名称" value={value.displayName} onChange={(displayName) => setValue({ ...value, displayName })} placeholder={user.username}/><label className="block text-sm font-medium">个人简介<textarea value={value.bio} onChange={(event) => setValue({ ...value, bio: event.target.value })} maxLength={500} placeholder="介绍一下你自己、正在做的事或擅长的方向" className="mt-1.5 block min-h-28 w-full rounded-lg border border-zinc-200 bg-transparent p-3 text-sm outline-none focus:border-zinc-500 dark:border-zinc-700"/></label><div className="grid gap-4 sm:grid-cols-2"><Field label="所在地" value={value.location} onChange={(location) => setValue({ ...value, location })} placeholder="重庆，中国"/><Field label="个人网站" value={value.website} onChange={(website) => setValue({ ...value, website })} placeholder="https://example.com"/></div></section>}{active === "security" && <section className="rounded-lg border border-zinc-200 bg-white p-6 dark:border-zinc-800 dark:bg-zinc-900"><div className="flex items-start gap-3"><MailCheck className="mt-0.5 size-5 text-emerald-600"/><div><h2 className="font-semibold">账户邮箱</h2><p className="mt-2 text-sm text-zinc-600 dark:text-zinc-400">{value.email} · {value.verified ? "已验证" : "尚未验证"}</p>{!value.verified && <Button className="mt-4" variant="outline" onPress={async () => { try { await api("/api/auth/verification", { method: "POST" }); setMessage("验证邮件已发送，请检查收件箱。") } catch (cause) { setMessage(cause instanceof Error ? cause.message : "发送失败。") } }}>发送验证邮件</Button>}</div></div></section>}{active === "notifications" && <section className="rounded-lg border border-zinc-200 bg-white p-6 dark:border-zinc-800 dark:bg-zinc-900"><h2 className="font-semibold">通知收件箱</h2><p className="mt-2 text-sm leading-6 text-zinc-500">Issue、拉取请求、Star 和 Fork 的站内通知会显示在顶部通知入口。邮件通知依赖管理员配置 SMTP 服务。</p></section>}{message && <p className={message.includes("已") ? "text-sm text-emerald-700" : "text-sm text-red-600"}>{message}</p>}{active === "profile" && <Button type="submit" isPending={saving}>保存个人资料</Button>}</form></div>
}
